# Система оценочных листов готовности к ОЗП

Веб-приложение для управления оценочными листами готовности объектов теплоснабжения к отопительному сезону.

## 🏗️ Структура проекта

```
assessment-sheets/
├── index.html              # Главная страница
├── css/
│   └── styles.css         # Все стили приложения
├── js/
│   ├── app.js             # Основная логика, загрузка данных, дерево
│   ├── dashboard.js       # Дашборд с KPI и таблицей объектов
│   ├── sheet.js           # Детальный просмотр оценочного листа
│   └── notifications.js   # Уведомления
└── data/
    ├── organizations.json # Иерархия: Комиссия→ОМСУ→ЕТО→ТСО→Объекты
    ├── sheets.json        # Оценочные листы с пунктами, файлами, комментариями
    └── notifications.json # Уведомления пользователя
```

## 🚀 Запуск

### Вариант 1: Локальный сервер (рекомендуется)

```bash
# Python 3
cd assessment-sheets
python -m http.server 8000

# Или Node.js
npx http-server -p 8000

# Откройте: http://localhost:8000
```

### Вариант 2: Расширение для браузера

1. Установите "Live Server" для VS Code
2. Откройте `index.html`
3. Клик правой кнопкой → "Open with Live Server"
### Вариант 3: Простое открытие
```bash
php -S localhost:8000
```
### Вариант 4: Простое открытие

⚠️ Могут не работать fetch запросы из-за CORS политики браузера.

## 📊 Структура данных

### Иерархия организаций

**Комиссия → ОМСУ → ЕТО → ТСО → Объекты (котельные, МКД)**

```
Региональная комиссия МО
  └─ Одинцовское городское округ (ОМСУ)
      └─ ЕТО Одинцовский г.о.
          └─ МУП "ЖКХ Назарьево" (ТСО)
              ├─ Котельная №1 п. Горки-2
              └─ Котельная №2 п. Горки-2
```

### Оценочный лист

Каждый объект имеет один оценочный лист на один период (год).

**Структура:**
- Древовидная иерархия пунктов (1, 1.1, 1.1.1, ...)
- К каждому пункту прикрепляются файлы
- Асинхронное согласование по цепочке: ТСО → ЕТО → ОМСУ → Комиссия
- Чат (комментарии) для каждого пункта

## 🎯 Основные функции

### 1. Навигация по дереву

- **Клик на ОМСУ/ЕТО/ТСО** → Дашборд с объектами этой организации
- **Клик на объект** → Детальный оценочный лист

### 2. Дашборд

**KPI карточки:**
- Всего объектов
- Средняя готовность (%)
- Количество объектов с замечаниями
- Средний индекс готовности

**Таблица объектов:**
- Прогресс заполнения с прогресс-баром
- Индекс готовности
- Статус (в работе / на проверке / есть замечания / готово)
- Последняя активность
- Кнопка "Открыть" → переход к детальному листу

**Фильтры:**
- Все
- С замечаниями
- Готовы

### 3. Детальный лист

**Метаданные:**
- № Листа
- Дата создания
- Индекс готовности
- Документов согласовано
- Общий прогресс (progress bar)

**Древовидная таблица пунктов:**
- Многоуровневая нумерация (раскрытие/скрытие подпунктов)
- Иконка файлов с количеством → модалка со списком файлов
- Цепочка статусов согласования (ТСО → ЕТО → ОМСУ → Комиссия)
- Оценка пункта (0 или 1)
- Комментарии с количеством → модалка с историей комментариев

**Цветовая индикация статусов:**
- 🟢 Зеленый - проверено/одобрено
- 🟡 Желтый - на проверке/активно
- 🔴 Красный - отклонено/замечания
- ⚪ Серый - ожидает

### 4. Уведомления

**Типы:**
- 💬 Новый комментарий
- ✓ Пункт одобрен
- ❌ Пункт отклонен
- ⚠️ Требуется внимание

**Клик на уведомление:**
- Переход к детальному листу
- Прокрутка к нужному пункту
- Подсветка пункта

### 5. Фильтр по периоду

Dropdown в левом верхнем углу sidebar:
- 2025-2026 (по умолчанию)
- 2024-2025
- 2023-2024

При изменении фильтруются все данные.

## 🎨 Дизайн

**Стиль:** Минималистичный корпоративный современный

**Цветовая палитра:**
- Primary (синий): `#2563eb`
- Success (зеленый): `#10b981`
- Warning (желтый): `#f59e0b`
- Danger (красный): `#ef4444`
- Gray: оттенки от `#f9fafb` до `#111827`

**Адаптивность:**
- Desktop: sidebar 320px + основной контент
- Tablet (<1024px): sidebar 280px
- Mobile (<768px): sidebar на всю ширину, контент под ним

## 📝 Редактирование данных

### Добавить ОМСУ

Отредактируйте `data/organizations.json`, добавьте новый объект в массив `omsu_list`:

```json
{
  "id": "omsu-4",
  "name": "Новый ОМСУ",
  "type": "omsu",
  "commission_id": "commission-1",
  "eto_list": []
}
```

### Добавить объект (котельную/МКД)

В нужном ТСО добавьте в массив `objects`:

```json
{
  "id": "obj-new",
  "name": "Новая котельная",
  "type": "boiler",
  "address": "Адрес",
  "tso_id": "tso-1"
}
```

### Добавить оценочный лист

В `data/sheets.json` добавьте в массив `sheets`:

```json
{
  "id": "9999",
  "object_id": "obj-new",
  "object_name": "Новая котельная",
  "period": "2025-2026",
  "tso_id": "tso-1",
  "progress": 0,
  "index": 0,
  "status": "working",
  "items": []
}
```

### Добавить уведомление

В `data/notifications.json` добавьте в массив `notifications`:

```json
{
  "id": "notif-new",
  "type": "comment",
  "object_id": "obj-3573",
  "sheet_id": "3573",
  "item_number": "1.1.1",
  "title": "Новый комментарий",
  "text": "Текст уведомления",
  "author": "Иванов И.И.",
  "author_role": "ЕТО",
  "created_at": "2024-11-06T10:00:00Z",
  "is_read": false
}
```

Обновите `unread_count`.

## 🔧 Кастомизация

### Изменить цвета

Отредактируйте переменные в `css/styles.css`:

```css
:root {
    --primary: #2563eb;
    --success: #10b981;
    /* ... */
}
```

### Изменить статусы

В `js/dashboard.js` и `js/sheet.js` найдите функции:
- `getStatusText(status)` - названия статусов
- `renderStatusChain(status)` - цепочка согласования

### Добавить новый тип организации

1. Обновите иерархию в `data/organizations.json`
2. Добавьте обработку в `js/app.js` → `renderTree()`
3. Добавьте логику в `js/dashboard.js` → `getSheetsForOrganization()`

## 📦 Зависимости

**Нет!** Чистый HTML/CSS/JS без фреймворков.

Используются только:
- Native JavaScript (ES6+)
- CSS Variables
- Fetch API для загрузки JSON

## 🐛 Известные ограничения

1. **Локальные данные** - вся информация в JSON файлах, нет backend
2. **Нет сохранения изменений** - изменения в UI не сохраняются
3. **Нет авторизации** - все данные доступны всем
4. **Нет валидации** - проверки данных минимальны

## 🚀 Интеграция с Backend

Для интеграции замените `fetch('data/*.json')` на API запросы:

```javascript
// Было:
const orgs = await fetch('data/organizations.json').then(r => r.json());

// Стало:
const orgs = await fetch('/api/organizations').then(r => r.json());
```

**Нужные API endpoints:**
- `GET /api/organizations` - иерархия организаций
- `GET /api/sheets?period=2025-2026` - оценочные листы
- `GET /api/sheets/:id` - детальный лист
- `GET /api/notifications` - уведомления
- `POST /api/sheets/:id/items/:itemId/files` - загрузка файла
- `POST /api/sheets/:id/items/:itemId/comments` - добавить комментарий

## 📞 Поддержка

Для вопросов и предложений создайте issue или свяжитесь с разработчиками.

---

**Версия:** 1.0.0  
**Дата:** Ноябрь 2024